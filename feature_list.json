{
  "project": "maia-landing-back",
  "version": "1.3.0",
  "updated": "2026-07-30",
  "legend": {
    "status": [
      "pending",
      "in_progress",
      "blocked",
      "done",
      "descartada"
    ],
    "rule": "Una feature a la vez. El implementer la pone in_progress; solo pasa a done tras APPROVED del reviewer. `descartada` = decisión del humano de no hacerla; la razón queda escrita para no volver a proponerla."
  },
  "features": [
    {
      "id": 9,
      "name": "CRUD de precios.",
      "description": "crear el crud para dar mantenimiento a la sección de precios de la página. esta sección incluye los planes y paquetes. Cada plan posee un % de descuento que debe ser parametrizable. El costo de cada plan debe manejar 2 precios, el mensual y el anual que recibe el descuento. Cada plan tiene descripciones, por lo que debe ser un campo de texto que se pueda mostrar en viñetas en el front. También cada plan debe incluir el calculo de ahorro pagando anualmente. Existen paquetes, cada uno tiene un nombre, descripción y precios.",
      "status": "pending"
    }
  ],
  "done": [
    {
      "id": 8,
      "name": "CRUD de usuarios",
      "status": "done",
      "closed": "2026-07-30",
      "note": "Mantenedor de usuarios del panel (crear, listar, ver, editar, eliminar) sobre el modelo plano de roles admin|editor de src/roles.js. ARCHIVOS NUEVOS: src/usersRouter.js (los 5 endpoints, cero SQL) y tests/users.test.js (53 tests, un describe por criterio del acceptance). TOCADOS: src/users.js (capa de datos ampliada), src/app.js (import + una linea de app.use), src/articlesRouter.js (cambio de permisos del DELETE, ver abajo), tests/articles.test.js (un test actualizado + 2 nuevos, 13 -> 15), docs/api-contract.md, docs/architecture.md (§2, §4, §5, §7, §8, §8.1 nueva, §11, §13), docs/database.md (§2, §5 + subseccion nueva de `users`, invariantes), docs/conventions.md (§3, §6), docs/context.md (§1, §3) y docs/verification.md (§1.1, §3 baseline, §6). CERO cambios de esquema (src/db.js no se toco: la tabla `users` conserva sus 6 columnas, verificado con test que ademas comprueba que db.js no menciona `activo`, que su unico ADD COLUMN sobre users sigue siendo el `role` de la feature 15 y que las tablas creadas siguen siendo las 4 conocidas) y CERO dependencias nuevas (bcryptjs y todo lo demas ya estaban; package.json, package-lock.json, render.yaml y .node-version intactos). Ninguna variable de entorno nueva; no se leyo ni se escribio ningun valor de .env. RUTAS, las cinco solo rol admin (401 sin cookie, 403 con rol editor, ambos con test sobre las 5): GET /api/admin/users (orden id ASC), GET /api/admin/users/:id, POST /api/admin/users (201; 422 {error,field} si falta email o password o si el role no es admin|editor; 409 si el email ya existe, reutilizando el 23505 -> 'email_taken' que ya manejaba users.js), PATCH /api/admin/users/:id (email, name, role y TAMBIEN password —decision del humano—; 404 / 409 / 422) y DELETE /api/admin/users/:id (204 sin body, 404). NO hay ninguna ruta publica de usuarios: /api/users responde 404, con test. INVARIANTE I3 — password_hash NUNCA sale por la API: src/users.js declara la lista fija PUBLIC_COLS = 'id, email, name, role, created_at' y TODAS las queries del CRUD la usan en su SELECT/RETURNING; cero SELECT * en src/users.js y src/usersRouter.js. La unica query que lee el hash sigue siendo la de findUserByEmail, porque el login lo necesita para verifyPassword — esa funcion NO se toco y NO se reutiliza en el CRUD. Es el mismo patron META_COLS/RAW_COLS que asento la feature 7. Dos tests: uno de comportamiento (listado, detalle, POST y PATCH, comprobando Object.keys y el TEXTO CRUDO de la respuesta, incluida la ausencia de cualquier $2[aby]$ suelto y del password en claro) y uno estructural (parsea PUBLIC_COLS del fuente y verifica que solo findUserByEmail lee password_hash). El reviewer auditó las siete queries una a una y lo dio por correcto. CONTRASENAS: SALT_ROUNDS = 12 vive UNA sola vez, en src/users.js; el re-hash del PATCH lo reutiliza (hashPassword) y el router nunca llama a bcrypt. El UPDATE usa password_hash = COALESCE($4::text, password_hash), asi que un PATCH sin password no toca el hash (test). Tests de que la vieja contrasena deja de servir, la nueva funciona y el hash persistido casa con /^\\$2[aby]\\$12\\$/. LAS DOS GUARDAS DEL DELETE, ambas 409 {error}: (a) nadie se borra a si mismo (comparacion contra req.user.id, que requireAuth recarga de la DB en cada request); (b) no se puede borrar al ULTIMO usuario con rol admin. COMO SE RESOLVIO LA CARRERA DE (b), que es lo importante: NO con un COUNT previo y suelto —dos borrados concurrentes de dos admins distintos leerian ambos 'quedan 2' y la tabla acabaria sin ningun admin—, ni con un unico DELETE ... AND (SELECT COUNT(*) ...) > 1 —bajo READ COMMITTED cada transaccion evalua la subconsulta contra su propio snapshot y las filas afectadas son distintas, asi que no se bloquean entre si—. deleteUser abre una transaccion (pool.connect + BEGIN) y bloquea en UNA SOLA sentencia con orden determinista: SELECT id, role FROM users WHERE role = 'admin' OR id = $2 ORDER BY id FOR UPDATE. Una sola sentencia con ORDER BY id evita el deadlock 40P01 que apareceria bloqueando primero el objetivo y despues el resto de admins (A esperaria a B y B a A); la segunda transaccion espera al COMMIT de la primera y, al desbloquearse, READ COMMITTED reevalua el WHERE sobre la version nueva de las filas, ya no ve al admin borrado, cuenta 1 y responde 409. countAdmins se llama DENTRO de esa transaccion pasandole el client (por eso acepta pool o client: misma interfaz .query), asi el numero no puede quedarse obsoleto entre el COUNT y el DELETE ni la funcion queda muerta. Ejercitado con dos DELETE HTTP CONCURRENTES reales (Promise.all, dos admins borrandose mutuamente): exactamente un 204 y un 409, y siempre queda >= 1 admin. El reviewer reprodujo la carrera en vivo y confirmo que la transaccion la cierra. Matiz anotado: por HTTP y sin concurrencia la guarda (b) es inalcanzable (quien llama ya es admin, asi que si el objetivo es otro admin hay >= 2, y si es uno mismo salta antes la guarda (a)), por eso hay ademas un test directo a la capa de datos (deleteUser rechaza con code 'last_admin' y la fila sobrevive). BORRADO FISICO Y ARTICULOS: se borra la fila, sin borrado logico ni columna `activo` ni cambios en el flujo de login (decision del humano). Borrar a un usuario NO borra sus articulos: articles.author_id es ON DELETE SET NULL (src/db.js:90). Demostrado con test real, no leyendo el DDL: crea el usuario, se loguea con el, publica un articulo (author_id sale de la sesion, no del body), borra el usuario y comprueba que la fila del usuario desaparecio, que el articulo sigue existiendo con su titulo, que su author_id quedo en NULL y que sigue sirviendose por la ruta publica. CAMBIO DE PERMISOS DEL EDITOR (decision explicita del humano): DELETE /api/admin/articles/:id (src/articlesRouter.js:105) pasa de requireRole('admin') a usar el adminGuard ya existente = [requireAuth, requireRole('admin','editor')], de forma que el editor completa 'ver, crear, editar y eliminar una publicacion'. docs/api-contract.md actualizado (la fila decia **admin**) con una nota que explica el cambio y avisa al front. El test tests/articles.test.js 'DELETE 403 con cookie editor (solo admin)' se ACTUALIZO a 'DELETE 204 con cookie editor (el editor si borra publicaciones, feature 8)' —comprobando ademas que la fila desaparece—, NO se borro, y se anadieron dos tests para no dejar el endpoint menos cubierto que antes (401 sin cookie con el articulo intacto, y 404 con cookie de editor si no existe): 13 -> 15 tests. Verificado por grep que ningun otro test del repo afirmaba 403 para el editor en esa ruta. El editor NO gana nada mas: sigue recibiendo 403 en las 5 rutas de usuarios y en las 3 de escritura de imagenes, solo lee leads (no existe DELETE de leads) y `editor` sigue siendo el DEFAULT de la columna role — los cuatro con test. OTROS DETALLES: createUser acepta ahora `role` (default 'editor', el mismo de la columna, asi que quien no lo pasa —scripts y tests previos— no cambia de comportamiento) y devuelve PUBLIC_COLS, lo que anade `role` a su retorno (cambio aditivo); scripts/seed-users.js ya le pasaba role y ahora lo aprovecha nativamente. updateUser es read-then-merge con lista de columnas FIJA (nada de SET dinamico, igual que updateImage), y valida role/email antes de tocar la BD. parseId se reutiliza tal cual de imagesRouter.js (regex ^\\d{1,19}$ + comparacion con PG_BIGINT_MAX por BigInt) para que un id no numerico o desbordado sea 404 y nunca un 500 por 22P02/22003, con test en las tres rutas con :id incluidos el limite exacto de bigint y el primer valor que lo excede. Validacion del email: PRESENCIA, no formato — el acceptance solo pedia 422 'si falta email'; anadir una regex habria sido un 422 no pedido. Se normaliza a lowercase+trim (test). Invariante I5 testeada explicitamente: se promueve un editor a admin y se le degrada de vuelta con la MISMA cookie, sin re-login, y la request siguiente refleja el rol nuevo. Los tests que dependen de cuantos admins hay en total corren en schemas efimeros propios (helper withFreshSchema) porque ese recuento es global a la tabla; y el rate limiter de login se desactiva SOLO en esa suite via createApp({ rateLimit: { auth: false } }) —para lo que existe esa opcion— porque la suite hace ~10 logins reales y el default de produccion es 10/15min por IP; los defaults de produccion no se tocan y el rate limiting sigue cubierto por tests/ratelimit.test.js. Los describes con trabajo real llevan timeout explicito de 20 s porque bcrypt de 12 rounds cuesta ~300 ms y con 12 archivos en paralelo se pasaba del testTimeout de 5 s de vitest (hubo 1 fallo intermitente antes de este ajuste; despues, corridas consecutivas en verde). REVIEW: APPROVED sin bloqueantes (progress/review_8.md); el reviewer verificó ejecutando —no leyendo el informe— I3, la carrera del ultimo admin, el criterio 8 y el cambio de permisos. De sus 7 menores se atendio el 2: los catch del POST y del PATCH loguaban solo err?.code || err?.message con una justificacion falsa ('para no loguear el body'), lo que dejaba sin traza un error inesperado de Postgres en contra de C8 y era incoherente con los otros tres handlers del archivo; ahora los cinco loguean err completo y el comentario dice la verdad (la contrasena en claro no puede aparecer porque NUNCA viaja a la base de datos: a la query va su hash). Test nuevo que provoca un 23514 real, captura console.error y comprueba que la traza lleva el codigo real y NO la contrasena. Residual escrito en el propio comentario del codigo: un error que haga que Postgres adjunte la fila (detail 'La fila que falla contiene (...)') logueria el HASH; hoy es inalcanzable porque la unica restriccion de users es el UNIQUE de email y ese caso (23505) se responde 409 antes del log. HUECO CONOCIDO QUE QUEDA ABIERTO (menor 1 del review, hueco de la ESPECIFICACION, no de la implementacion): el ultimo admin puede AUTO-DEGRADARSE con PATCH { role: 'editor' } y dejar el sistema con cero admins, en un solo request y sin concurrencia — el mismo estado que la guarda del DELETE existe para impedir, y sin vuelta atras por la API. El reviewer lo reprodujo en vivo. NO se arreglo aqui a proposito: el acceptance enumera taxativamente las dos guardas requeridas, ambas sobre el DELETE, y el criterio 6 no pide ninguna restriccion sobre el cambio de role; ni docs/api-contract.md ni docs/architecture.md prometen mas de lo que el codigo hace (sin discrepancia doc<->codigo), y meterlo habria sido scope creep (C12). Queda como FEATURE DE SEGUIMIENTO que abre el lider: guarda last_admin en updateUser, dentro de la misma transaccion con FOR UPDATE que ya existe en deleteUser. Los menores 3 (domainError('email_required') de updateUser sin mapear en el catch del PATCH: daria 500 en vez de 422, hoy inalcanzable por HTTP), 4 (String(body.email) convierte un objeto en '[object Object]'), 5 (updateUser read-then-merge sin transaccion: lost update con dos PATCH concurrentes, mismo trade-off aceptado en updateImage) y 6 (tercera copia de parseId/PG_BIGINT_MAX: extraerla seria un refactor oportunista, material para una feature de limpieza junto con positiveNumberFromEnv) quedan ANOTADOS a proposito, no arreglados. El menor 7 (C15) ya no aplica: el humano autorizo commitear esta feature; el implementer no commiteo ni pusheo nada y el commit lo hace el lider al cerrar (rama y mensaje sugeridos: feat/8-crud-usuarios y 'feat(users): CRUD de usuarios y roles del panel'). VERIFICACION FINAL: npm test -> 12 archivos / 221 tests, verde (baseline previo 11/166; +53 del archivo nuevo y +2 netos en articles.test.js, ninguno borrado ni marcado skip — grep de .skip/.todo/xit/.only sin resultados). npm run test:no-db sigue en 3 archivos / 30 tests: users.test.js necesita Postgres. Los 14 criterios del acceptance tienen test que los ejercita (mapeo criterio->it() en progress/history.md y en review_8.md §4). No se modifico ../maia-landing-front ni ../maia-landing: consumir estos endpoints desde el panel es trabajo de otro repo."
    },
    {
      "id": 7,
      "name": "CRUD de imagenes.",
      "status": "done",
      "closed": "2026-07-29",
      "note": "Mantenedor de las imágenes de las secciones de la landing (hero = carrusel de 3, cta_final = imagen de MaIA), con el binario REAL en Postgres. ARCHIVOS NUEVOS: src/images.js (capa de datos: createImage, listImages, getImageById, getImageWithBytes, updateImage, deleteImage + helpers puros SECCIONES, MIME_WHITELIST, isAllowedMime, extensionOf, extensionMatchesMime, sniffMime, isValidSeccion; no importa express ni toca req/res), src/imagesRouter.js (los 5 endpoints; cero SQL) y tests/images.test.js (49 tests). TOCADOS: src/db.js (CREATE TABLE IF NOT EXISTS images + CREATE INDEX IF NOT EXISTS images_seccion_orden_idx dentro de ensureSchema; cero DROP, cero ALTER, cero mutación de datos — el reviewer verificó la idempotencia corriendo ensureSchema 3 veces con una fila insertada en medio), src/app.js (solo el import y app.use(createImagesRouter({ pool, schema, requireAuth, maxFileSize: options.images?.maxFileSize }))), .env.example, docs/database.md, docs/architecture.md (§2, §4, §5, §7, §7.1 nueva, §11, §12, §13), docs/api-contract.md, docs/conventions.md (§3, §5), docs/verification.md (§1.1, §3 baseline, §6) y docs/context.md (§1, §3). DECISIÓN DEL BINARIO EN POSTGRES (BYTEA), tomada por el humano y no re-litigada: el filesystem de Render es efímero, un archivo escrito en disco se pierde en cada deploy, y S3/Cloudinary exigiría credenciales nuevas que hoy no existen; se acepta el peso en la BD. Se usa multer con memoryStorage, así que el buffer va directo de la request a la columna sin tocar disco. RUTAS: público GET /api/images (?seccion=, orden `orden ASC, id ASC`) y GET /api/images/:id/raw; solo rol admin POST /api/admin/images (multipart/form-data), PATCH /api/admin/images/:id y DELETE /api/admin/images/:id (204 sin body). Las TRES rutas de escritura responden 401 sin cookie y 403 con rol editor; los dos GET son públicos. El acceptance original decía POST /api/images y '400' genérico: el líder lo corrigió a mitad de sesión al prefijo /api/admin y a los códigos del repo. CÓDIGOS: 422 { error, field } si falta el archivo, falta `seccion`, `seccion` no está en el enum o `orden` no es entero >= 0; 415 { error, field: 'file' } si el MIME no pasa la validación; 413 { error, field: 'file' } si el archivo excede el límite. Los tres salen con Content-Type application/json — el MulterError se atrapa explícitamente en el wrapper uploadSingleImage (LIMIT_FILE_SIZE → 413, otros MulterError → 422, cualquier otro error → next(err) como red de seguridad del errorHandler); sin ese wrapper multer haría next(err) y el cliente vería un 500. El reviewer ejercitó los 6 casos en vivo y confirmó que ninguno devuelve HTML ni 500. VALIDACIÓN DE MIME EN TRES CAPAS, porque el mimetype que entrega multer es el Content-Type que declara el cliente y es falsificable: (1) MIME declarado en la whitelist, (2) extensión del filename coherente con ese MIME (ambas en el fileFilter), (3) MAGIC BYTES del buffer real (sniffMime: firma PNG de 8 bytes, FF D8 FF de JPEG, RIFF….WEBP de WebP, escritos a mano sin dependencias nuevas) y que coincidan con el declarado. Lo que se persiste en mime_type es el MIME DETECTADO, no el declarado. LÍMITE DECLARADO DE ESA VALIDACIÓN: no decodifica la imagen, así que un archivo con cabecera válida y cuerpo basura se aceptaría y el navegador simplemente no lo renderizaría; decodificar exigiría una librería de imagen que no está aprobada. Con nosniff + whitelist sin SVG el riesgo residual es una imagen rota, no ejecución de código. SVG EXCLUIDO A PROPÓSITO (no es una omisión que haya que 'arreglar'): un SVG es XML ejecutable (<script>, onload/onerror, <foreignObject>, xlink:href='javascript:') y GET /api/images/:id/raw lo serviría en crudo con su propio Content-Type desde el origen de la API → XSS almacenado en el mismo origen donde vive la cookie de sesión del panel. El motivo está escrito en docs/architecture.md §7.1 (sección propia, no un comentario del código), reforzado en la tabla de decisiones y en docs/api-contract.md, y hay un test que exige que el doc lo explique y mencione XSS. Si algún día hace falta un vectorial, se decide entonces con su propio sanitizador. ANÁLOGO DE LA INVARIANTE I3 PARA `bytes`: el binario NUNCA sale por la API en JSON. src/images.js tiene dos listas de columnas — META_COLS (sin bytes) que usan las 5 queries de listado/detalle/escritura, y RAW_COLS (con bytes) usada solo por getImageWithBytes(), a la que solo llama GET /api/images/:id/raw. Cero SELECT * en todo src/. Dos tests: uno de comportamiento (recorre las filas, comprueba Object.keys y el texto crudo de la respuesta con /\"bytes\"/, distinguiendo correctamente size_bytes — que sí sale — de bytes — que no —, repetido en las respuestas del POST y del PATCH) y uno estructural (parsea META_COLS/RAW_COLS del propio fuente). Escrito como invariante en docs/database.md y docs/architecture.md §7. El reviewer auditó las 6 queries una por una y lo dio por correcto sin reservas. OTRAS DECISIONES: `seccion` es un enum en código (SECCIONES = hero, cta_final), no un CHECK en la BD, para que añadir una sección sea una línea y no una migración; el PATCH solo toca alt/orden/seccion y nunca el binario (reemplazar una imagen es un POST nuevo), y updateImage hace read-then-merge-then-UPDATE con lista de columnas FIJA — no construye un SET dinámico, precisamente para que no haya dónde colar una interpolación; el límite de tamaño sigue el criterio de src/rateLimit.js (default 5 MB activo sin configurar nada, override por IMAGES_MAX_FILE_SIZE_BYTES leída DENTRO de la factory —nunca en tiempo de import, C3— o por createApp({ images: { maxFileSize } }), que es lo que usan los tests con un límite de 128 bytes para ejercitar el 413); multer se monta SOLO en POST /api/admin/images y DESPUÉS del adminGuard, nunca global, así que un anónimo o un editor recibe 401/403 sin que el binario se parsee, y el límite de las imágenes queda independiente del limit 32kb de express.json; endurecimiento del /raw con X-Content-Type-Options: nosniff y saneado del filename (solo basename, sin caracteres de control ni comillas) para que un nombre hostil no rompa una cabecera. VARIABLE DE ENTORNO NUEVA: IMAGES_MAX_FILE_SIZE_BYTES, documentada por NOMBRE y propósito en .env.example y en docs/architecture.md §12; es opcional (default 5 MB activo) y no es un secreto, así que no necesita sync: false. No se leyó ni se escribió ningún valor de .env en toda la sesión. DEPENDENCIAS: multer ^2.2.0 es la ÚNICA añadida — una sola línea en dependencies de package.json; el script test, test:no-db, el resto de dependencies y devDependencies quedaron intactos, y render.yaml y .node-version no se tocaron (mtime 2026-06-04, verificado por el reviewer). package-lock.json trae exactamente 10 entradas nuevas y todas son multer o sus transitivas. REVIEW: APPROVED sin bloqueantes (progress/review_7.md); el reviewer auditó bytes, la validación de MIME y los errores de subida EJERCITANDO LA APP, no leyendo el informe del implementer, y los tres salieron correctos. De sus 6 hallazgos menores se atendieron los dos que marcó como 'merecen atenderse antes de cerrar': (1) parseId filtraba lo no numérico pero NO el desbordamiento de bigint, así que un id de solo dígitos fuera de rango llegaba a Postgres, provocaba el error 22003 y acababa en 500 en vez de 404 en las tres rutas con :id, contradiciendo la promesa literal de docs/api-contract.md ('404 … nunca un 500'); se arregló EN EL CÓDIGO (no bajando la promesa del doc): parseId ahora exige /^\\d{1,19}$/ y compara con PG_BIGINT_MAX = 9223372036854775807n vía BigInt, con 2 tests nuevos que ejercitan el id del repro del reviewer en /raw, PATCH y DELETE más el límite exacto de bigint, el primer valor que lo excede y un id de 20 dígitos; (2) se borró la sección '## Verificación' duplicada de progress/current.md, resto de plantilla que decía '(pendiente)' y contradecía al bloque real. Los menores 3 (updateImage sin transacción: read-then-write puede perder un campo con dos PATCH concurrentes; riesgo ~0 con un panel de un solo admin y el trade-off es correcto porque la alternativa —SET dinámico— es justo lo que evita la interpolación de SQL), 4 (tercera copia de positiveNumberFromEnv, idéntica a las de app.js y auth.js: extraerla habría sido un refactor oportunista y una violación de C12 — candidata para una feature de limpieza futura) y 6 (recuento de criterios) quedan ANOTADOS a propósito, no arreglados. El menor 5 era un error de redacción del propio acceptance ('las cuatro rutas de escritura' cuando solo hay tres): el implementer lo declaró en vez de inventarse una cuarta ruta, el reviewer confirmó por grep que no había ninguna escapada, y el leader corrigió el texto del criterio. ALCANCE: el reviewer verificó por mtimes que solo se escribieron los archivos de esta feature; en particular src/articlesRouter.js NO se tocó y su DELETE sigue con requireRole('admin') — la feature 8 no se adelantó. VERIFICACIÓN FINAL: npm test → 11 archivos / 166 tests, verde (baseline previo 10 / 117; +49 del archivo nuevo, ninguno borrado ni marcado skip, grep de .skip/.todo/xit sin resultados). npm run test:no-db sigue en 3 archivos / 30 tests: images.test.js necesita Postgres y no entra en el subconjunto en seco. Los 14 criterios del acceptance tienen test que los ejercita (mapeo criterio→it() en progress/history.md y en review_7.md §3). C15 queda incumplido a conciencia: NO se commiteó ni se pusheó nada y no se creó rama — es la instrucción explícita del humano, la misma excepción ya aceptada al cerrar las features 1, 3, 4 y 5; el trabajo queda en el árbol local, y la rama/commit sugeridos son feat/7-crud-imagenes y 'feat(images): CRUD de imágenes de secciones con binario en Postgres'. No se modificó ../maia-landing-front ni ../maia-landing: migrar Hero.tsx/CTAFinal.tsx para consumir esta API queda FUERA de alcance y es trabajo de otro repo."
    },
    {
      "id": 4,
      "name": "Suite unitaria que corra sin Postgres",
      "status": "done",
      "closed": "2026-07-28",
      "note": "npm run test:no-db (nuevo script en package.json, única línea añadida; `test`, dependencies y devDependencies intactos) corre `vitest run tests/phone.test.js tests/email.test.js tests/roles.test.js` — 30 tests, ninguno abre `db.js` ni un `pool`. `tests/roles.test.js` (feature ya cerrada, auditado igual por si acaso) resultó mixto: sus 3 describes puros (ROLES, hasRole, requireRole) se quedaron en el archivo; el único describe que abría Postgres (`ensureSchema añade columna `role` a users`, 2 tests) se movió intacto (mismo SQL, mismas aserciones) a `tests/roles.schema.test.js` nuevo. `tests/app.test.js` (feature 2) y `tests/ratelimit.test.js` (feature 3) se dejaron íntegros fuera del comando rápido — su `beforeAll` de nivel de archivo abre Postgres para todo el archivo aunque parte de sus asserts no lo necesiten; partirlos habría sido más invasivo que lo que pide el acceptance ('phone, email y validación pura'), documentado como decisión explícita. Verificado con `TEST_DATABASE_URL` apuntando a un puerto muerto (127.0.0.1:1): `npm run test:no-db` pasa igual (30/30) mientras que cualquier suite con DB real falla con ECONNREFUSED bajo la misma variable — confirma que el aislamiento es real. `npm test` → 10 archivos / 117 tests, verde (mismo total que antes del split, solo repartido en un archivo más). `docs/verification.md` (nueva §1.1, baseline actualizado, checklist), `docs/architecture.md` (nueva §11.1, §11 recuento actualizado, §14 reescrito: refresh de sesión y tests-sin-Postgres movidos a Resueltas, .env y ensureSchema clarificados) y `docs/context.md` §4 actualizados para no contradecir el nuevo comando. Aprobado por el reviewer sin bloqueantes ni hallazgos menores (progress/review_4.md): verificó `package.json` línea a línea, el split de roles.test.js it() por it(), corrió test:no-db con Postgres inalcanzable más el control negativo, y npm test completo. No se tocó `render.yaml`, `.node-version` ni ninguna dependencia. No se leyó ni escribió ningún valor de `.env`; esta feature no introduce variables de entorno nuevas. No se commiteó ni pusheó nada."
    },
    {
      "id": 1,
      "name": "Sacar .env del control de versiones",
      "status": "done",
      "closed": "2026-07-28",
      "note": "ADVERTENCIA — ESTO NO CIERRA LA EXPOSICIÓN: `git rm --cached .env` saca el archivo del índice de git (verificado: `git ls-files .env` vacío) pero el repo `maia-landing-back` es PÚBLICO en GitHub y `.env` estuvo trackeado desde antes de que existiera la regla de `.gitignore`. Sus valores reales (DATABASE_URL, SMTP_PASS, SMTP_USER, AUTH_SECRET, MAIA_ADMIN_PASSWORD) han sido legibles por cualquiera todo este tiempo y SIGUEN en el historial de commits de git — el saneo del índice no los borra ni los invalida, solo evita que se sigan añadiendo cambios futuros de `.env` a git. La única mitigación real es que un humano ROTE cada una de esas credenciales en su proveedor (Render Postgres/Neon/Supabase, el proveedor SMTP o Resend, regenerar AUTH_SECRET, cambiar MAIA_ADMIN_PASSWORD) y, opcionalmente, purgue el historial de git (`git filter-repo`/BFG) si quiere borrar el rastro — ninguna de las dos cosas la hace un agente. Quien lea esta nota dentro de meses: si esas credenciales no se han rotado, la exposición sigue activa aunque `.env` ya no esté trackeado. Detalle técnico: `.env` sigue existiendo en disco (server arranca igual, verificado con GET /api/health → 200) y `.env.example` documenta las 21 variables usadas por el código, ninguna con valor real, incluyendo `NODE_ENV` (gap detectado y corregido en esta sesión) y la convención `SMTP_HOST=resend` para la API HTTP de Resend. `npm test` → 9/9 suites, 117/117 tests. No se commiteó ni pusheó nada (decisión explícita del humano): el cambio queda en el índice/árbol de trabajo local para que él lo revise y decida cuándo commitear. Aprobado por el reviewer sin bloqueantes (progress/review_1.md); único hallazgo fue una autocrítica de proceso del propio reviewer (uso accidental de `git diff --cached` sin `--name-only` que mostró 2 líneas de valores reales en su propia sesión de review, no atribuible al implementer, sin filtración a ningún archivo)."
    },
    {
      "id": 6,
      "name": "Health check que no dependa del boot para el schema",
      "status": "descartada",
      "closed": "2026-07-28",
      "note": "Decisión del humano: se mantiene el comportamiento actual (fail-fast). Si `ensureSchema` falla al arrancar, el proceso muere con exit(1) y en Render eso es un deploy que no levanta, con el error visible en los logs. El arranque tolerante se descartó porque dejaría el servicio 'arriba' respondiendo 503 aunque estuviera mal configurado, que enmascara el fallo en vez de exponerlo. No volver a proponerla sin un motivo nuevo: la limitación de architecture.md §14 (ensureSchema acopla el boot a la disponibilidad de la DB) se acepta conscientemente."
    },
    {
      "id": 0,
      "name": "Logos PNG en plantillas de correo",
      "status": "done",
      "closed": "2026-07-27",
      "note": "docs/images/*.png presentes; el cargador prefiere .png sobre .svg. Cierra architecture.md §14.1."
    },
    {
      "id": 2,
      "name": "Middleware de errores JSON",
      "status": "done",
      "closed": "2026-07-27",
      "note": "errorHandler (4 args) montado al final de src/app.js, después del 404. asyncHandler (src/asyncHandler.js) envuelve los 14 puntos de registro async de la app (app.js, auth.js, articlesRouter.js, leadsRouter.js) para que un throw async no capturado no deje la request colgada. tests/app.test.js cubre throw síncrono, throw async, headersSent, JSON malformado, 404 sin cambios, y un throw real en POST /api/auth/login vía createApp(). docs/api-contract.md y docs/conventions.md §7 actualizados. Aprobado en segunda pasada de review tras corregir cobertura incompleta detectada en la primera (progress/review_2.md). Cierra architecture.md §14.3."
    },
    {
      "id": 3,
      "name": "Rate limiting en endpoints públicos",
      "status": "done",
      "closed": "2026-07-27",
      "note": "src/rateLimit.js (nuevo): createRateLimiter(), contador de ventana fija en memoria sin dependencias nuevas, reloj inyectable (now), poda perezoza gatillada por tamaño (pruneExpired/maybeSweep, SWEEP_THRESHOLD=5000). Aplicado solo a POST /api/contact (src/app.js) y POST /api/auth/login (src/auth.js, opción rateLimiter inyectada desde createApp), nunca de forma global. Defaults activos sin configurar nada: CONTACT_RATE_LIMIT_MAX=20/60s, AUTH_RATE_LIMIT_MAX=10/15min — toleran el volumen de tests/contact.test.js y tests/auth.test.js sin tocarlos. trust proxy=1 en createApp (Render es un único reverse proxy; trust proxy=true permitiría spoofing de X-Forwarded-For). 429 { error } sin cabeceras RateLimit-*/Retry-After. Inyectable/desactivable vía createApp({ rateLimit: { contact|auth: {...}|false } }). tests/ratelimit.test.js (17 tests) cubre los 5 criterios de acceptance más el hallazgo menor 1 del review (poda del Map, __internals expuesto solo para tests). docs/api-contract.md, docs/architecture.md (§2, §4, §11, §12, §13, §14) y .env.example actualizados. Aprobado por el reviewer (progress/review_3.md) sin bloqueantes; hallazgo menor 1 (poda no testeada) atendido antes de cerrar. Hallazgo menor 2 (el supuesto de que Render añade X-Forwarded-For no se puede verificar sin desplegar) queda como deuda conocida, requiere un request real contra el servicio desplegado. Cierra architecture.md §14.2."
    },
    {
      "id": 5,
      "name": "Refresh / renovación de sesión",
      "status": "done",
      "closed": "2026-07-27",
      "note": "src/auth.js: renewCookieIfNeeded() + loadSession() (reemplaza loadUserFromCookie, ahora también devuelve el payload decodificado). Nuevas opciones de factory refreshWindowMs (lee AUTH_REFRESH_WINDOW_MS, default 1 día) y now (reloj inyectable, solo para decidir si toca renovar, no para firmar el token nuevo). Integrado en requireAuth y GET /api/auth/me; POST /api/auth/login y /logout no la disparan. Reutiliza literalmente el mismo objeto cookieOpts que ya usa el login (invariante I6 intacta, verificado en modo producción con cookieSecure: true). Un token ya expirado nunca llega a renewCookieIfNeeded (verifyToken lo rechaza antes → 401 sin Set-Cookie). El rol se sigue recargando desde DB en cada request (invariante I5 intacta). tests/auth.test.js (7 tests nuevos, describe 'Renovación de sesión (feature 5)') cubre los 4 criterios de acceptance más las trampas de la consigna: no renovar fuera de ventana, no renovar en logout, cookie renovada conserva opciones exactas en producción. docs/api-contract.md, docs/architecture.md (§8, §12) y .env.example actualizados. Aprobado por el reviewer sin bloqueantes (progress/review_5.md); 2 hallazgos menores: 1) la tabla 'Archivos tocados' de progress/current.md no listaba docs/api-contract.md pese a haberlo modificado — corregido antes de cerrar; 2) el guard remainingMs <= 0 de renewCookieIfNeeded es código defensivo hoy inalcanzable en el flujo normal (un token expirado nunca llega ahí porque verifyToken ya lo filtra antes), sin test directo que lo ejercite — aceptado como cinturón y tirantes, no bloqueante. Cierra architecture.md §14.2 (punto 'Sin refresh de sesión')."
    }
  ]
}
